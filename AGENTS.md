# Agent instructions

- Avoid styling, formatting, or cosmetic UI changes that are not directly required to implement the current task.
- When a task requires a UI update, keep unrelated styles and component presentation unchanged.
